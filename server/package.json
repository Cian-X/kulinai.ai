{
  "name": "kulina-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
	"prestart": "cd ../client && npm ci && npm run build",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "migrate": "node migrate.js",
	"seed": "node seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
